// NOTE: the Patient Appointment hooks (auto-create token + "Token Queue" button)
// live in public/js/patient_appointment.js, registered via hooks.doctype_js —
// this Token Counter form itself needs no extra client script beyond the default.
